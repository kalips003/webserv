#include "Ft_Post.hpp"
#include "_colors.h"

#include "HttpRequest.hpp"
///////////////////////////////////////////////////////////////////////////////]

void	Ft_Post::function1() {

// 	const std::string& c_type = getRequest().find_setting("content-type");
// // Content-Type: multipart/form-data; boundary=----geckoformboundaryeb47a963ac0d54a6202a6abec127252
// 	size_t pos = c_type.find("multipart/form-data;");
// 	if (pos == std::string::npos)
// 		; // error
// 	const std::string b("boundary=");
// 	pos = c_type.find(b);
// 	std::string boundary = "--" + c_type.substr(pos + b.size());
// 	std::string end_boundary = boundary + "--";
	


}

void 	f_read() {
	// int fd = open(path.c_str(), O_RDONLY);
	// char buf[4096];
	// ssize_t r;

	// while ((r = read(fd, buf, sizeof(buf))) > 0) {
	// 	data.append(buf, r);   // data = std::string or vector<char>
	// }

	// close(fd);
}
