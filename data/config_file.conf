# server.conf

# global settings
server_name myserver.local
listen 9999
root /var/www/html
index index.html

# optional per-location settings
location /images {
    root /var/www/images; hello cat
    cgi /usr/bin/python3}

location /api {
    cgi /usr/bin/python3
}
